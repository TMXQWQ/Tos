#include "inc.h"

int main(){
	// kmm_init();
    logs("[Warm]KMM was in Unadble!",_LOG_WARM);
    //vfs-init();
    logs("[Warm]VFS was in Unadble!",_LOG_WARM);
    while(1){
        // __asm__ __volatile__ (\
        // "nop\r\t"
        // "hlt\r\t"
        // );

    }
    // return 0;    //never return
}
